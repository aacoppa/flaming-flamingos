June 5th
* Remake the makefile
* Create interface
    - object struct
        * Different types of collision based on object id
    - plane object
        * type of object
    - world.c
        * list of objects
            - including boundaries
        * generic update objects
        * draw object function
    - collide function
    - generic move function
        * update matrix of each object
        * local transformation matrix
